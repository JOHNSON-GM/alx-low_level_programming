#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * main - printing lowercase alphabet in reverse
 *
 * Return: Always 0 (success)
 *
 */
int main(void)
{
	int X;

	for (X = 122; X > 96; X--)

	{

		putchar(X);

	}
	putchar('\n');
	return (0);
}
